import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		width: 200,
		margin: 10,
	},

	image: {
		width: "100%",
		height: 175,
	},

	text: {
		color: "grey",
		marginTop: 12,
	},
});

export default styles;
